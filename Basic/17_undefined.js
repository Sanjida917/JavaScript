

// Undefined variable - If we Declare a variable but don't put any on it, this is an undefined type variable.
 let firstName ;
 console.log("First Name Variable Type is : "+typeof firstName);

 var lastname ; //It is also possible with var keyword using with variables
 console.log("Last Name Variable Type Is : "+typeof lastName);

//(const fullName;) - It isn't possible because const' declarations must be initialized.

fullName = "Sanjida Mahfuj";
console.log(fullName+" Is A :",typeof fullName);